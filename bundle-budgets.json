{
  "$comment": "First Load JS budgets in bytes (gzipped, matches what `next build` reports as the 'First Load JS' column and what users actually pay on the wire). Set ~25% above current sizes to catch regressions while leaving room for legitimate growth. Update via PR if you intentionally grow a route.",
  "version": 1,
  "firstLoadJs": {
    "/": 200000,
    "/achievements": 350000,
    "/algorithms": 450000,
    "/patterns": 450000,
    "/structures": 450000,
    "/visualizer/[category]/[slug]": 500000,
    "/compare": 450000,
    "/compare/[category]/[a]/[b]": 600000,
    "/reference": 350000
  }
}
